import Transction from "../models/transaction.model";

export const addTransction= async(request, response)=>  {
    try {
        const {amount, type, category, descriptions} = request.body;

        if()
    } catch (error) {
        
    }
}